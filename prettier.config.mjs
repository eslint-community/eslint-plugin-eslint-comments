/**
 * @import { type Config } from "prettier"
 */

/**
 * @satisfies {Config}
 */
const prettierConfig = {
    semi: false,
    tabWidth: 4,
    trailingComma: "es5",
}

export default prettierConfig
